{
  "name": "chargenote-backend",
  "version": "0.1.0",
  "private": true,
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "prestart": "npm run build",
    "start": "node server.js",
    "dev": "vite",
    "server": "cross-env NODE_ENV=development node server.js",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.34.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "cross-env": "^7.0.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
